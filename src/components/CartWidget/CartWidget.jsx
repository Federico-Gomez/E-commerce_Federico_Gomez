import react from "./assets/cart-fill.svg"

const CartWidget = () => {

    return(
        <div>
            <img src={react} alt="cart-widget" />
            0
        </div>
    )
}

export default CartWidget